name: base
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.13
  - conda==25.5.0
  - setuptools
  - pip
  - wheel
  - conda-anaconda-telemetry
  - conda-anaconda-tos[version='>=0.1.3']
  - conda-content-trust
  - conda-libmamba-solver
  - menuinst
  - anaconda-anon-usage
  # custom installs from here, everything above was added by the devcontainer:
  - jupyterlab
  - nbdev
prefix: /opt/conda
